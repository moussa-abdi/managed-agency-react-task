import React from "react";
import Typography from "@mui/material/Typography";

const Headline = () => (
  <div
    style={{
      display: "flex",
      justifyContent: "center",
      flexDirection: "column",
      alignItems: "center",
    }}
  >
    <Typography variant="h3">Managed agency selection</Typography>
    <Typography variant="body1">Strengthen your onboarding process</Typography>
  </div>
);

export default Headline;
